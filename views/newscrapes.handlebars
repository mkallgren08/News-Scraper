{{>navPanel}}
<h1>Scrape Complete</h1>
  

<div class="row">
  <div class="container">
    <h3>New Articles Here</h3>
    {{#each result}}
      <div class="panel panel-default">
        <div class="panel-heading">
          <h3 class="panel-title"><a href = "{{this.link}}">{{this.title}}</a></h3>
        </div>
        <div class="panel-body">
          Panel content
        </div>
      </div>
    {{/each}}
  </div>
</div>